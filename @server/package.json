{
  "name": "remote-site-server",
  "version": "1.0.0",
  "description": "A simple Node.js server to act as the remote site for the Cordova Native Fallback plugin example.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
